# =============================================================================
# METALLB CONFIGURATION (IPAddressPool + L2Advertisement)
# =============================================================================
# Docs: https://metallb.io/configuration/
#
# Applied after MetalLB CRDs are ready.
# Actual config is in: manifests/network/metallb-config/config.yaml
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    path: manifests/network/metallb-config
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: metallb-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
